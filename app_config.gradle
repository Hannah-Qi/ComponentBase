ext {
    //发布（集成）模式--true--library   组件化模式--false--app
    isRelese = true

    //定义一个包名，存放APT生产的Kt文件
    packageForAPT = "customrouter_apt"

    app_android = [
            compileSdk : 34,
            minSdk : 24
    ]
}